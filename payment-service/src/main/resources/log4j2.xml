<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO" monitorInterval="30">
    <!-- Properties -->
    <Properties>
        <Property name="serviceName">payment-service</Property>
        <Property name="logstashHost">${env:LOGSTASH_HOST:-localhost}</Property>
        <Property name="logstashPort">${env:LOGSTASH_PORT:-5000}</Property>
    </Properties>

    <!-- Appenders -->
    <Appenders>
        <!-- Console Appender (for local debugging) -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!-- Socket Appender to Logstash (JSON format) -->
        <Socket name="Logstash"
                host="${logstashHost}"
                port="${logstashPort}"
                protocol="TCP"
                reconnectionDelayMillis="10000">

            <!-- JSON Template Layout -->
            <JsonLayout compact="true" eventEol="true" properties="true" includeStacktrace="true">
                <KeyValuePair key="service_name" value="${serviceName}"/>
                <KeyValuePair key="environment" value="${env:ENVIRONMENT:-docker}"/>
            </JsonLayout>

            <!-- Alternative: Custom JSON Layout -->
            <!-- Uncomment this and comment above if you want custom format -->
            <!--
            <JsonLayout compact="true" eventEol="true" properties="true">
                <KeyValuePair key="service.name" value="${serviceName}"/>
            </JsonLayout>
            -->
        </Socket>

        <!-- Async Wrapper for better performance -->
        <Async name="AsyncLogstash" includeLocation="true">
            <AppenderRef ref="Logstash"/>
        </Async>
    </Appenders>

    <!-- Loggers -->
    <Loggers>
        <!-- Application Loggers -->
        <Logger name="com.example.payment" level="DEBUG" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AsyncLogstash"/>
        </Logger>

        <!-- Spring Framework -->
        <Logger name="org.springframework" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AsyncLogstash"/>
        </Logger>

        <!-- Root Logger -->
        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AsyncLogstash"/>
        </Root>
    </Loggers>
</Configuration>